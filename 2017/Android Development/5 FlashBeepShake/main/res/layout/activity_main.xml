<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:tools="http://schemas.android.com/tools"
android:id="@+id/activity_main"
android:layout_width="match_parent"
android:layout_height="match_parent"
android:paddingBottom="@dimen/activity_vertical_margin"
android:paddingLeft="@dimen/activity_horizontal_margin"
android:paddingRight="@dimen/activity_horizontal_margin"
android:paddingTop="@dimen/activity_vertical_margin"
tools:context="com.example.admiralfresh.flashandstuff.MainActivity">


<Button
    android:text="Flash"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_centerHorizontal="true"
    android:layout_marginTop="46dp"
    android:id="@+id/FlashButton"
    android:onClick="onFlashClick"
    />

<Button
    android:text="Vibrate"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_below="@+id/FlashButton"
    android:layout_centerHorizontal="true"
    android:layout_marginTop="46dp"
    android:id="@+id/VibrateButton"
    android:onClick="onVibrateButton"
    />

<Button
    android:text="Beep"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_below="@+id/VibrateButton"
    android:layout_alignStart="@+id/VibrateButton"
    android:layout_marginTop="47dp"
    android:id="@+id/beepButton"
    android:onClick="onBeepButton"
    />

<Button
    android:text="Do All"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_alignParentBottom="true"
    android:layout_alignStart="@+id/beepButton"
    android:layout_marginBottom="71dp"
    android:id="@+id/doAllButtonID"
    android:onClick="doAllButton"
    />
</RelativeLayout>
